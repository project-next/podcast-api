spring:
  datasource:
    url: jdbc:h2:mem:PODCAST_DB;MODE=POSTGRESQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
  h2:
    console:
      enabled: false

auth:
  admin:
    username: Javi
    password: Changeme!
  generic-user:
    username: Tyler
    password: Changeme!

server:
  timezone: America/Chicago